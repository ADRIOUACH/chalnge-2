#include <stdio.h>
#include <stdlib.h>

int main()
{
    int nombre;
    printf("entre un nombre");
    scanf("%d",&nombre);
    if(nombre % 2 == 0){
            printf("%d ce nombre est paire\n",nombre);
  }  else{
        printf("%d ce nombre est impaire\n",nombre);
  }
    return 0;
}
